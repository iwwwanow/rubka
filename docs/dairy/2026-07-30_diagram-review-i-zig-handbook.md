# 2026-07-30

- Заведён `CLAUDE.md` в корне репозитория: проект ученический, Claude не пишет
  и не запускает Zig-код (даже boilerplate), только объясняет и предлагает
  команды. Пользователь пишет/собирает/коммитит сам.
- Создан `docs/agents/zig-handbook.md` — общие сведения о языке и таблица
  основных команд (`zig init`, `zig build`, `zig build run`, `zig fmt` и т.д.).
  Установленная версия — 0.16.0, отмечено что API свежее большинства туториалов
  (новый `std.Io`, `std.process.Init` в сигнатуре `main`).
- Несколько итераций ревью `docs/diagram.d2` (hexagonal / ports & adapters):
  - найден и исправлен разрыв инверсии зависимостей — `use-cases`, а затем
    `cli`, били напрямую в `adapters.sway`, минуя `ports.window-manager`;
  - убрано дублирование use-case на каждое направление (`move-window-left`/
    `-right` → один `move-window(direction)`);
  - поправлен стиль: параметр вынесен из id в label + комментарий;
  - удалены мёртвые закомментированные рёбра, выровнены кавычки в id.
  - итог: чистая схема `cli.move-window.{left,right} → use-cases.move-window
    → ports.window-manager ← adapters.sway`.
- Обсуждался вопрос — не сменить ли стартовый use-case с `move-window` на
  нотификации (нужны всё равно для ошибок). Вывод: не менять — нативные
  desktop-нотификации это D-Bus, протокол сложнее i3 IPC (SASL-хендшейк,
  бинарный маршалинг с выравниванием), так что это не упрощение. Для ошибок
  сейчас, если нужно, дешевле обернуть `notify-send` через subprocess, не
  строя под это отдельный протокольный слой.
- Добавлено правило про дневник в `CLAUDE.md`: `docs/dairy/`,
  `сегодняшняя-дата_суть-задачи.md`.
