- [ ] добить диаграмму:

```
- Проблема с прошлого раза не решилась, а переехала: теперь cli.move-window (и left, и right) бьёт напрямую в adapters.sway, минуя порт. То есть infrastructure всё ещё имеет прямую линию к конкретному адаптеру в обход application — тот же разрыв инверсии зависимостей, только источник сместился с use-case на cli.
- Стилистически: id "move-window (direction)" смешивает имя и описание параметра прямо в идентификаторе — лучше move-window как id, параметризацию оставить в комментарии/label.
```
